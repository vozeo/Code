#include <bits/stdc++.h>
using namespace std;
#define rep(i_, s_, t_) for (int i_ = (s_); i_ <= (t_); ++i_)

int main() {
    read(n), read(m), read(K);
    rep (i, 1, n)
        rep (j, 1, m)
            read(a[i][j]);
    rep (i, 1, n) {
        hd = tl = 0;
        rep (j, 1, m) {
            while (hd < j)


        }
    }


}
